query debugNewsList($ouIds: [String], $limit: Int) {
  newsEntityQuery(ouIds: $ouIds, limit: $limit) {
    items {
      title
      lead
      url {
        path
      }
      date
      organizationId
    }
  }
}
